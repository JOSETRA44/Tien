<resources>
    <!-- Dark variant. `@color/window_background` already resolves to the dark
         value through values-night/colors.xml; only the status-bar icon
         polarity has to change here. -->
    <style name="Theme.Tien" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
